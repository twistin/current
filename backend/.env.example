# Copia este archivo a .env y ajusta los valores.
# NUNCA commits .env al repositorio.

DATABASE_URL=postgres://current:current_secret@localhost:5432/current_dev

# Puerto donde escucha el servidor
PORT=3000

# Nivel de logs: trace | debug | info | warn | error
RUST_LOG=current_api=debug,tower_http=info
